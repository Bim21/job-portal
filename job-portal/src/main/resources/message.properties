# -----------------------------------------------------
# FOR UserCreateForm OR UserUpdateForm
# -----------------------------------------------------
UserForm.id.NotNull=User id must be NOT null.
UserForm.id.Exists=User is NOT exists by id yet.
UserForm.username.NotBlank=User username must be NOT blank.
UserForm.username.Length=User username's length is max 50 characters.
UserForm.username.NotExists=User is exists by username already.
UserForm.username.Exists=User username is NOT exists by name yet.
UserForm.password.NotBlank=User password must be NOT blank.
UserForm.password.Length= User password's length is max 32 characters.
UserForm.email.NotBlank= User email must be NOT blank.
UserForm.email.Length=User email's length is max 32 characters.
UserForm.email.NotExists=User email is exists by email already.
UserForm.email.Exists=User email is NOT exists by email yet.
UserForm.firstName.NotBlank=User firstName must be NOT blank.
UserForm.firstName.Length=User firstName's length is max 50 characters.
UserForm.lastName.NotBlank=User lastName must be NOT blank.
UserForm.lastName.Length= User lastName's length is max 50 characters.

# -----------------------------------------------------
# FOR RecruiterCreateForm OR RecruiterUpdateForm
# -----------------------------------------------------


